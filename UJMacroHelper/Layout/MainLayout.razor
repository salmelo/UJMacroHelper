@inherits LayoutComponentBase
<div class="page">
    @*     <div class="sidebar">
    <NavMenu />
    </div>

    <main>
    <div class="top-row px-4">
    <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
    </div>

    <article class="content px-4">
    @Body
    </article>
    </main> *@
    <BasicRollHelper UseSelected="@UseSelected" />
    <CombatRollsHelper UseSelected="@UseSelected" />
    <AttackRollsHelper UseSelected="@UseSelected" />
    <GenericRollHelper UseSelected="@UseSelected" />
    <label>
        Use Selected Token
        <InputCheckbox @bind-Value="@UseSelected" />
    </label>
    <NPCRollHelper UseSelected="@UseSelected"/>
</div>

@code {
    public bool UseSelected { get; set; }
}
